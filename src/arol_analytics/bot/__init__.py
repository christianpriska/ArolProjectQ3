# Layer 4: terminal chat interface over the Layer-2 analytics tools (guided
# menu mode) and the Layer-3 AROLAgent (free natural-language mode). No
# external account or network service needed -- run with
# `python -m arol_analytics.bot`.
